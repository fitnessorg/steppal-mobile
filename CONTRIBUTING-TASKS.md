# Contributor Tasks

Every `TODO(contributor)` comment in the codebase, collected here as GitHub-issue-ready entries.
Grouped easiest first within each phase. If you start one, comment on the matching issue (or open one
using this entry as the body) so work doesn't collide.

_(Phase 1 has no contributor tasks — the app shell, tooling, and CI are fully built. Tasks will be
added here as later phases land unfinished pieces.)_
